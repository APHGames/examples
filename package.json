{
  "scripts": {
    "start": "copyfiles -f static/*.* www/ && parcel examples/*.html --out-dir www",
    "start_no_map": "copyfiles -f static/*.* www/ && parcel examples/*.html --out-dir www --no-source-maps",
    "deploy": "copyfiles -f static/*.* dist/ && parcel build examples/*.html --no-source-maps"
  },
  "devDependencies": {
    "@types/pixi.js": "^4.7.1",
    "parcel-bundler": "1.12.3",
    "parcel-plugin-json-url-loader": "^0.1.1",
    "pixi.js": "^4.7.3",
    "typescript": "^3.1.1"
  },
  "dependencies": {
    "copyfiles": "^2.1.0"
  }
}
