{
  "name": "mi-aph-materials",
  "author": "Adam Vesecky",
  "scripts": {
    "start": "node scripts/copy.js && tsc --noEmit && parcel templates/*.html templates/*.pug --out-dir build",
    "deploy": "node scripts/copy.js && tsc --noEmit && parcel build templates/*.html templates/*.pug --out-dir build --no-cache --no-source-maps",
    "lint": "tslint --project tslint.json"
  },
  "devDependencies": {
    "@types/matter-js": "^0.10.4",
    "parcel-bundler": "^1.12.3",
    "parcel-plugin-json-url-loader": "^0.1.2",
    "pug": "^2.0.4",
    "sass": "^1.23.0",
    "tslint": "^5.20.0",
    "typescript": "^3.6.3"
  },
  "dependencies": {
    "matter-js": "^0.14.2",
    "pixi-sound": "^3.0.3",
    "pixi.js": "5.1.3",
    "poly-decomp": "^0.3.0"
  }
}
